# Infraestrutura

Os arquivos de execução local estão no `docker-compose.yml` da raiz. Este diretório receberá configurações de Nginx, infraestrutura como código e manifests de ambiente quando essas integrações forem implementadas.
